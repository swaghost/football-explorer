/**
 * Shared Styles Index
 *
 * This directory provides a centralized stylesheet system for the entire application.
 * All D3UIV6 components, toolbars, and dialogs should import these shared styles.
 *
 * Quick Start:
 * -----------
 * Import the main shared stylesheet in your Angular component:
 *
 * @Component({
 *   styleUrls: ['../../../styles/_shared.scss', './component.scss']
 * })
 *
 * Available Stylesheets:
 * ---------------------
 * _shared.scss        - Main entry point (imports everything)
 * base/_variables.scss - CSS custom properties and design tokens
 * base/_typography.scss - Typography styles and utilities
 * base/_utilities.scss - Layout and utility classes
 * components/_toolbar-base.scss - Shared toolbar component styles
 * components/_buttons.scss - Button components and variants
 *
 * Key Features:
 * ------------
 * ✅ CSS Custom Properties for consistent theming
 * ✅ Automatic dark mode support
 * ✅ Standardized component architecture
 * ✅ Utility-first approach for rapid development
 * ✅ Mobile-responsive design patterns
 * ✅ Accessibility-focused components
 *
 * Design Tokens:
 * -------------
 * Colors: --color-primary, --color-secondary, --color-success, etc.
 * Typography: --font-family-base, --font-size-*, --font-weight-*
 * Spacing: --spacing-1 through --spacing-16 (4px increments)
 * Shadows: --shadow-sm, --shadow-base, --shadow-lg, --shadow-xl
 * Borders: --radius-sm, --radius-base, --radius-lg, --radius-xl
 * Z-index: --z-base, --z-overlay, --z-dropdown, --z-modal
 *
 * Common Classes:
 * --------------
 * Layout: .flex, .grid, .items-center, .justify-between, .gap-*
 * Spacing: .p-*, .m-*, .pt-*, .pb-*, .pl-*, .pr-*
 * Typography: .text-*, .font-*, .tracking-*, .leading-*
 * Components: .btn, .draggable-toolbar, .panel-header, .panel-content
 *
 * For detailed documentation, see README.md in this directory.
 */

// This file serves as documentation only
// Import the SCSS files directly in your Angular components:
// styleUrls: ['../../../styles/_shared.scss', './component.scss']
