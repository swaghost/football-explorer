# Sample Asset Files

This directory contains sample files to demonstrate the assets folder structure.

For testing purposes, you can add:

- `sample-logo.png` in the images folder
- `sample-icon.svg` in the icons folder
- `sample-data.json` in the data folder
- Custom fonts in the fonts folder

These files will be accessible at `/assets/[subfolder]/[filename]` in your application.
